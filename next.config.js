/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.coingecko.com', 'coin-images.coingecko.com'], // API'den gelen görsellerin domaini
  },
};

module.exports = nextConfig;
